#include "main.h"

/**
 * _strlen - This check for the length of the given string
 * @s: This collects a string pointer
 * Return: a value.
 */
int _strlen(char *s)
{
	int length = strlen(s);

	return (length);
}
